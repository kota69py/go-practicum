package calc

import "errors"

// Max はスライスの最大値を返します。空スライスの場合は error を返します。
func Max(numbers []int) (int, error) {
	if len(numbers) == 0 {
		return 0, errors.New("empty slice")
	}
	max := numbers[0]
	for _, n := range numbers[1:] {
		if n > max {
			max = n
		}
	}
	return max, nil
}
