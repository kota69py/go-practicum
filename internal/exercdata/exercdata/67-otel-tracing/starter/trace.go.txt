package main

import (
	"context"
	"net/http"

	"go.opentelemetry.io/otel/trace"
)

// InitTracerProvider creates and returns a TracerProvider with stdout exporter.
// Returns a cleanup function that should be called on shutdown.
func InitTracerProvider() (trace.TracerProvider, func()) {
	panic("not implemented")
}

// StartSpan starts a new span with the given name.
func StartSpan(ctx context.Context, name string) (context.Context, trace.Span) {
	panic("not implemented")
}

// GetTraceID extracts the trace ID from the span in context.
func GetTraceID(ctx context.Context) string {
	panic("not implemented")
}

// TraceMiddleware is HTTP middleware that creates spans for each request.
func TraceMiddleware(next http.Handler) http.Handler {
	panic("not implemented")
}

// ChildOperation demonstrates parent-child span relationship.
func ChildOperation(ctx context.Context, name string) {
	panic("not implemented")
}
